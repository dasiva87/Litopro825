# ========================================
# CLOUDFLARE R2 - CONFIGURACIÓN
# ========================================
# Instrucciones:
# 1. Copia estas líneas y pégalas en tu archivo .env
# 2. Reemplaza los valores de ejemplo con tus credenciales reales de Cloudflare R2
# 3. Puedes obtener estas credenciales en: https://dash.cloudflare.com > R2 > Manage R2 API Tokens

# Disco de archivos principal (cambia de 'local' a 'r2')
FILESYSTEM_DISK=r2

# CLOUDFLARE R2 - Credenciales
# Obtén estos valores al crear un API Token en Cloudflare R2
R2_ACCESS_KEY_ID=tu_access_key_id_aqui
R2_SECRET_ACCESS_KEY=tu_secret_access_key_aqui
R2_BUCKET=grafired-uploads
R2_ENDPOINT=https://xxx.r2.cloudflarestorage.com
R2_REGION=auto
R2_URL=https://pub-xxx.r2.dev

# Nota: R2_URL es la URL pública del bucket (opcional, solo si configuras dominio público)
# Si no tienes dominio público configurado, déjala vacía o usa el endpoint de R2

# ========================================
# PASOS PARA OBTENER LAS CREDENCIALES:
# ========================================
# 1. Ve a https://dash.cloudflare.com
# 2. Click en "R2" en el menú lateral
# 3. Click en "Manage R2 API Tokens"
# 4. Click en "Create API Token"
# 5. Configura:
#    - Token Name: grafired-laravel
#    - Permissions: Object Read & Write
#    - Bucket: grafired-uploads (o tu bucket)
# 6. Copia el Access Key ID y Secret Access Key
# 7. Copia el Endpoint (ejemplo: https://abc123.r2.cloudflarestorage.com)
# 8. Pega los valores en las variables de arriba

# ========================================
# CONFIGURACIÓN DE URL PÚBLICA (OPCIONAL)
# ========================================
# Si quieres que las imágenes sean públicas con dominio personalizado:
# 1. En Cloudflare R2, ve a tu bucket
# 2. Settings > Public Access > Allow Access
# 3. Copia la URL pública (ejemplo: https://pub-abc123.r2.dev)
# 4. Pégala en R2_URL
